# SSH Extension

This extension provides a tool to execute SSH commands.

## `ssh` tool

- **Description:** Executes an SSH command.
- **Usage:** The tool takes a single string argument `command` which should be the full SSH command to execute.
- **Example:** `ssh(command: "user@host 'ls -l'")`
