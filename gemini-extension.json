{
  "name": "ssh",
  "version": "2.1",
  "description": "An extension to run remote SSH commands and transfer files using the Paramiko library.",
  "tools": [
    {
      "local_tool": {
        "command": ["python", "ssh_tool.py"],
        "input_type": "JSON"
      },
      "function_declarations": [
        {
          "name": "ssh",
          "description": "Executes a command on a remote host.",
          "parameters": {
            "type": "OBJECT",
            "properties": {
              "args": {
                "type": "STRING",
                "description": "The SSH arguments in the format 'user@hostname \"command\"'."
              }
            },
            "required": ["args"]
          }
        }
      ]
    }
  ]
}
